<div class="container text-center">
    <!-- Hero Section -->
    <section class="hero fade-in-up" id="hero">
        <div class="section-header">
             <span class="section-tag">Smart care for modern dog owners.</span>
            <h1>
                Everything for your <span class="highlight">dog</span>, in one beautiful app
            </h1>
            <p>
                Track appointments, manage documents, get instant AI-powered advice, and share your dog's profile with vets and trainers—all in one delightful place.
            </p>
            <div class="cta-group">
                <a href="#cta" class="btn search-btn">
                    Start Your Journey
                    <span>→</span>
                </a>
                <a href="#how-it-works" class="btn cancel-btn">
                    Learn More
                </a>
            </div>
        </div>
        </div>
        <div class="hero-image">
          <%= image_tag "dogoutline1.png",
              class: "img-fluid banner-image my-5",
              style: "height: 600px;",
              alt: "Outline of a playful Puppy" %>
        </div>
    </section>
    <!-- Features Section -->
    <section class="features fade-in-up", style="background-color: #5C3A5E;" id="features">
        <div class="section-header">
            <span class="section-tag">🎯 Why Choose DogLog?</span>
            <h2>Everything you need, nothing you don't</h2>
            <p class="section-description">
                Designed specifically for busy pet owners who want the best for their dogs without the hassle.
            </p>
        </div>
        <div class="features-grid">
            <div class="feature-card">
                <span class="feature-icon">📋</span>
                <h3>Simple Care Management</h3>
                <p>Keep track of your dog's appointments, notes, and reminders. Quickly share any information with anyone that needs it.</p>
            </div>
            <div class="feature-card">
                <span class="feature-icon">🤖</span>
                <h3>On-Demand AI Support</h3>
                <p>From quick training advice to guidance on something not quite right, get instant answers to your pet care questions with our AI assistant.</p>
            </div>
            <div class="feature-card">
                <span class="feature-icon">📅</span>
                <h3>Smart Scheduling</h3>
                <p>Never miss a vet appointment, training session, or grooming date. Set reminders and get notifications for all your dog's important dates.</p>
            </div>
            <div class="feature-card">
                <span class="feature-icon">🔒</span>
                <h3>Secure Sharing</h3>
                <p>Share your dog's profile with vets, trainers, or dog sitters with customizable security options. PIN protection, view limits, and expiration dates included.</p>
            </div>
        </div>
    </section>

    <!-- How It Works -->
    <section class="how-it-works fade-in-up" id="how-it-works">
        <div class="section-header">
            <span class="section-tag">💡 Getting Started</span>
            <h2>Your journey in three simple steps</h2>
        </div>
        <div class="steps">
            <div class="step">
                <div class="step-number">1</div>
                <h3>Create Your Profile</h3>
                <p>Add your dog's basic information, photos, and important details like breed, weight, and microchip number.</p>
            </div>
            <div class="step">
                <div class="step-number">2</div>
                <h3>Add Documents & Events</h3>
                <p>Upload vaccination records, training certificates, and schedule upcoming appointments all in one place.</p>
            </div>
            <div class="step">
                <div class="step-number">3</div>
                <h3>Share & Manage</h3>
                <p>Easily share your dog's profile with vets, trainers, and caregivers. Get AI-powered advice whenever you need it.</p>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section fade-in-up" id="cta">
        <div class="cta-content">
            <h2>Ready to give your dog the best care?</h2>
            <p>Join thousands of happy dog owners who trust DogLog for their pet care management.</p>
            <a href="<%= new_household_path %>" class="btn btn-lg" style="background-color: #F6A623; font-size: 1.2rem; padding: 1.2rem 3rem;">
                Get Started Free
                <span>→</span>
            </a>
        </div>
    </section>
</div>
     <!-- Footer -->
    <footer class="bg-light py-4 mt-5">
      <div class="container text-center">
        <p class="text-muted mb-0">&copy; <%= Date.current.year %> DogLog. All rights reserved.</p>
      </div>
    </footer>

     <script>
        // Smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Intersection Observer for fade-in animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in-up').forEach(el => {
            observer.observe(el);
        });

        // Add stagger effect to feature cards
        const featureCards = document.querySelectorAll('.feature-card');
        featureCards.forEach((card, index) => {
            card.style.animationDelay = `${index * 0.1}s`;
        });
    </script>
