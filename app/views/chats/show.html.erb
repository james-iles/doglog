<div class="d-flex flex-column" style="height: calc(100vh - 80px);">
  <div class="container bg-white border-bottom">
    <div class="d-flex align-items-center gap-2 py-2 mb-2">
      <% if @chat.dog.photo.attached? %>
        <%= image_tag @chat.dog.photo, class: "rounded-circle", style: "width: 40px; height: 40px; object-fit: cover;", alt: @chat.dog.name %>
      <% end %>
      <h1 class="mb-0" id="chat_title"><%= @chat.title %></h1>
    </div>
    <div class="d-flex justify-content-between align-items-center gap-3 pb-2">
      <%= button_to save_summary_chat_path(@chat), method: :post, class: "btn p-0" do %>
        <i class="fa-solid fa-file-arrow-down text-success me-1"></i> Save Summary
      <% end %>
      <%= button_to chat_path(@chat), method: :delete, class: "btn p-0",
          data: { turbo_confirm: "End chat without saving?" } do %>
        <i class="fa-solid fa-xmark fa-lg text-danger me-1"></i> End Chat
      <% end %>
    </div>
      </div>

  <div class="flex-grow-1 overflow-auto container" id="messages" data-controller="scroll-bottom">
    <% @chat.messages.each do |message| %>
      <% if message.role == "user" %>
        <div class="d-flex justify-content-end mb-2">
          <div class="bg-white px-3 pb-1 pt-1 rounded-pill">
            <%= message.content %>
          </div>
        </div>
      <% elsif message.role == "assistant" %>
        <div class="d-flex justify-content-start mb-3">
          <div class="px-3 pb-0 pt-3">
            <%= message.content %>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>

  <div class="container py-2 mb-1">
    <%= simple_form_for [@chat, @message], html: { class: "d-flex align-items-stretch gap-2" } do |f| %>
      <div class="flex-grow-1 mb-0">
        <%= f.input :content, label: false, required: true, placeholder: "Let's chat about #{@chat.dog.name}...", wrapper: false, input_html: { class: "form-control h-100 bg-white" } %>
      </div>
      <%= f.button :submit, "Ask", class: 'btn btn-primary border border-dark' %>
    <% end %>
  </div>
</div>
